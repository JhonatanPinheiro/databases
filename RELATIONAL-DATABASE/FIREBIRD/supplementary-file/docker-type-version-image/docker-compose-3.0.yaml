services:
  firebird30:
    image: firebirdsql/firebird:3.0.11
    container_name: db_container_firebird
    restart: always
    environment:
      FIREBIRD_USER: SYSDBA
      FIREBIRD_PASSWORD: masterkey
    ports:
      - "3050:3050"
    volumes:
      - ./firebird_data:/var/lib/firebird/data
      - ./firebird_restore:/firebird/restore
